{
  "name": "life-data",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "cli": "doppler run -- bun src/cli.ts",
    "test:watch": "doppler run -- bun test --watch",
    "test": "doppler run -- bun test"
  },
  "devDependencies": {
    "bun-types": "^1.0.17",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.14",
    "axios": "^1.6.2",
    "commander": "^11.1.0",
    "dotenv": "^16.3.1",
    "google-spreadsheet": "^4.1.1",
    "watch": "^1.0.2"
  }
}
