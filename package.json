{
  "name": "life-data",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "cli:dev": "doppler run -- bun src/cli.ts",
    "cli": "bun src/cli.ts",
    "test:watch": "doppler run -- bun test --watch",
    "test": "doppler run -- bun test",
    "job:ls": "act -l",
    "job:dev": "doppler run --mount .env -- act -j"
  },
  "devDependencies": {
    "bun-types": "^1.0.17",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.14",
    "commander": "^11.1.0",
    "google-spreadsheet": "^4.1.1"
  }
}
